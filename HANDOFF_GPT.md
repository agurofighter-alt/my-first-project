# HANDOFF — 蠱毒プロジェクトのGPT実装向け引き継ぎ

> 本書は他モデル(GPT等)でこの型を実装する人/AIへの引き継ぎ。型の正本= `rules.md` v1.6(暫定)。**型は設計上モデル非依存**(§1・§2「配役はモデル非依存の役割」)——本書はClaude固有の実行層をGPTの等価物に置き換える地図。

## 1. 渡すものの構成
| 層 | ファイル | 移植 |
|---|---|---|
| **憲法(コア)** | `rules.md` v1.6 | そのまま使う(§12「Claude Code 実行ノート」のみ実行層=読み替え対象) |
| **壺クラス** | `profiles/*.md`(企画/コード/専門/レイヤー2) | そのまま |
| **起動キット** | `templates/*.md`(shared_context/task/progress/state/seal_linter/検分指示書/移送/Skill/runbook) | そのまま(ファイル契約はモデル非依存) |
| **参照実装** | `pot_A/`(縮小・歴史壺)・`pot_B/`(本番規模100→20→5→1の完走例) | 読むだけ(プロンプト実物・検分書・台帳が全部残っている) |
| **監査記録** | `red_team/`(87所見と裁定)・`chosa/`(思想乖離・完成検収・迷子検査) | 設計判断の theirs/why が全部ここ |

## 2. Claude→GPT 実装マップ
| 型の概念 | Claudeでの実装(参照実装) | GPTでの等価物 |
|---|---|---|
| 壺師(分解・統合) | メイン会話(上位モデル) | オーケストレータ(上位モデルの1会話 or スクリプト) |
| 蟲師・課・補助課(並列ワーカー) | サブエージェント(context分離) | **ステートレスAPI呼び出し**——会話履歴なし、入力は〈shared_contextの中身+taskの中身+出力書式〉のみ。**この入力最小化=密閉の実装**であり、便利でも他の情報を渡さないこと |
| 目付(独立監査) | 別コンテキスト+**別基底モデル**(v1.6) | 別会話+**別モデルファミリー**。GPT実装なら監査をClaude系等の他社モデルに——同一基底の監査5回が見逃した6件を別基底が1回で検出した実測がある(pot_B/metsuke_kenbun_06) |
| 舌(人間) | チャットの人間 | 人間UI(応答6種: 選ぶ/選ばない/差し戻し/軸宣言/複数/曖昧——**曖昧は機械が解釈しない**) |
| 乱数(層化圧縮・提示順) | `shuf --random-source=<(openssl enc -aes-256-ctr -pass pass:SEED ...)` | seed付きPRNGなら何でも可。**seedと結果をstate.mdに必ず記録**(監査可能性の要) |
| ファイル契約 | task_XX.md=実験の独立変数として保存必須、progress=台帳付き、検分書=監査役の自筆が正本 | そのまま(git推奨——版と歴史の保存が監査の土台) |

## 3. 実測済みの落とし穴(2壺+監査9回ぶんの授業料。詳細= `templates/orchestration_runbook.md`)
1. **機械の自己申告は常に実態より良い**——「全行引用付き」(実際7行無引用)・「全親処理済み」(実際3親未処理)が実測。台帳の**独立再計数**を監査役に必ずやらせる。ワーカーの戻り値だけで完了と判断しない。
2. **オーケストレータの転記・要約は最大欠陥工程**——2壺連続で9件/9件の転記ズレ、要約時に存在しない機構を書き足す事故も実測。数値は再転記せず**正本参照方式**(計数表を指す)にする。版上げは「宣言と本文の同時更新」を徹底(差分ブロックだけの版上げ事故が2回起きた)。
3. **監査指示に申告を混ぜない**——指示書に書いた前提が監査の枠を歪めた実測あり。検分指示は定型書式(`templates/metsuke_directive_template.md`)。
4. タグ表記の統一([未検証]の別表記が計数を壊す)・部分完了の検出(再開規定)・commitは着弾ごと小刻みに。
5. **監査コスト≧生成コスト**を見込む(実測: 監査1,113k tokens > 生成892k)。これは価格であって無駄ではない——検出実績が毎回あった。

## 4. 未解決のまま渡るもの(実装しても消えない)
- **牙の問題(最重要)**: 封印(絶対点・審美・閾値の禁止)を厳格に守ると、淘汰の合法手段が枯れて**選別がほぼ抽選になる**(非乱数圧縮力3%の実測)。対策候補=舌の軸宣言/成文法の濃い題材(コード壺)/倍率の生成側寄せ——**どれを選ぶかは舌(人間)の裁定事項**。`chosa/shiso_drift.md` 末尾の「舌が決めるべき分岐10件」を実装前に読むこと。
- **批准状態**: rules v1.3〜v1.6は機械起案の**暫定**(§15: 発効は舌の批准)。GPT実装の開始時に、舌が (a)批准して v1.6 から始める (b)v1.2原文(`git show 1961f7b:rules.md`)から始めて必要な裁定だけ拾う——のどちらかを明示的に選ぶこと。
- §14の保留事項・安全帳票のクローズ(人間確認)・レイヤー2/コード壺/専門壺の実走例なし。

## 5. 入手
- リポジトリ: `agurofighter-alt/my-first-project` ブランチ `claude/kodoku-rules-v1-2-pw2d9c`(全履歴・全検分書込み)
- 単一ファイルバンドル(コア一式の連結・GPTに直接貼れる): 生成コマンド=
  `for f in rules.md profiles/*.md templates/*.md HANDOFF_GPT.md; do echo -e "\n\n===== FILE: $f =====\n"; cat "$f"; done > kodoku_bundle.md`
